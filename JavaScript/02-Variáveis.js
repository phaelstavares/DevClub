/*
    Variáveis:
    - let -> Pode alterar o valor quando quiser
    - const -> Valor NÃO pode ser declarado NOVAMENTE
    - var -> DESCONTINUADO / NÃO USAR

    variável é um pedaço de memória que está sendo guardado
*/

const numeroDeAlunosDaSalaSete = 40
// não comece com números e nem espaçamento ao nomear uma variável.

console.log(numeroDeAlunosDaSalaSete)